{
  "name": "surge-client",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "watch": "watchify surge-client.js --s Surge -o ./dist/surge-client.js",
    "build" : "browserify surge-client.js --s Surge | uglifyjs -mc > ./dist/surge-client.min.js"
  },
  "dependencies": {
    "sockjs-client": "^1.0.0"
  },
  "devDependencies": {
    "browserify": "latest",
    "uglify-js": "^2.4.23",
    "watchify": "latest"
  }
}
